# Salesforce DX Antigravity Rules

- Always verify LWC JavaScript with `npm run lint` and `npm test` after editing components.
- Run `npm run prettier` to ensure code matches project formatting conventions.
- Keep Apex code bulkified, secure (`WITH USER_MODE`), and properly tested.
- Maintain `force-app/main/default` structure for metadata.
